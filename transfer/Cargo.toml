[package]
name = "filebeam-transfer"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Pure transfer coordination policies for Filebeam clients"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
