# This directory is the development image's build context (see compose.yaml).
# Keep it independent from the root production-context allowlist.
*
!Dockerfile
!Caddyfile
!php.ini
!supervisord.conf
!start-container
