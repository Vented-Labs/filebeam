<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <permission
        android:name="io.filebeam.android.permission.TEST_DOCUMENTS"
        android:protectionLevel="signature" />
    <!-- Disposable emulator acceptance instances use HTTP on the host bridge. -->
    <application
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic">
        <!-- Debug-only evidence entry point. It renders the production root and ViewModel. -->
        <activity
            android:name=".FixtureActivity"
            android:exported="false" />
        <provider
            android:name=".TestDocumentsProvider"
            android:authorities="io.filebeam.android.testdocuments"
            android:exported="true"
            android:grantUriPermissions="true"
            android:readPermission="io.filebeam.android.permission.TEST_DOCUMENTS" />
    </application>
</manifest>
